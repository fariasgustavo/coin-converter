import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#505454',
        alignItems: 'center',
        justifyContent: 'center',
    },

    currency_item: {
        color: "#000"
    },
});

export default styles;