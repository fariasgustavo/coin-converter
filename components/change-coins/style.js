import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container_change_coins:{
        alignItems: 'center',
        justifyContent: 'center',
        flexDirection: 'row',
    },  
    text_change_coins: {
        color: '#fff',
    },
    image_change:{
        resizeMode: 'contain',
        width: 20,
        marginRight: 10,
        tintColor: '#fff'
    }
});

export default styles;