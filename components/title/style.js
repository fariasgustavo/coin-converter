import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title: {
        fontSize: 24,
        color: '#fff',
        marginTop: 20,
        marginBottom: 20
    }
});

export default styles;